import React from 'react';
import prereloaded from '../../assets/images/prereloaded.svg';

let Preloaded = (props) => {
    return (
        <img src={prereloaded}/>
    );
}

export default Preloaded;